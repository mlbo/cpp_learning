# C++ 35 天教程文档重构计划（v4.0：仅 Markdown）

> 本计划用于本轮文档专项：保留 Day 1-35 的主题、顺序和学习量，只修改 Markdown，通过“单一信息源 + 当日差异 + 前后链接”减少重复，并依据可靠的 C++ 资料补齐不完整讲解。

## 1. 本轮目标与边界

### 1.1 目标

1. 让同一个知识点只有一个主要讲解位置，其他文档说明本处用途并链接过去。
2. 保留每天既有主题、算法题、EMC++ 条款、工程动作和练习闭环，不用删内容来制造“简洁”。
3. 为只有结论或代码的章节补齐动机、正式语义、成立前提、边界、反例、复杂度和自测。
4. 统一 C++11/14/17/20 的版本标签，仓库构建基线仍是 C++17。
5. 让根导航、总规划、专题教程、周 README、每日 README、题解 README 各自承担稳定且不冲突的职责。
6. 所有引用都能追溯到标准参考、Core Guidelines 或优秀教材，不大段复制受版权保护的教材内容。

### 1.2 只允许的修改

- 根目录教学 Markdown。
- `tutorials/*.md`。
- `week_01` 至 `week_05` 内的教学 README、专题指南和题解 README。
- 必要时调整 Markdown 内的相对链接、目录、表格、代码片段和参考资料。

### 1.3 明确不修改

- 任何 `.cpp`、`.h`、`CMakeLists.txt`、`.cmake`、`.sh`、配置文件或生成文件。
- `CHECK_REPORT.md`、`FIX_REPORT.md`、`CONTENT_CHECK_REPORT.md` 等历史检查报告。
- `docs/README.md` 及当前工作区已有的 docs 脚本改动。
- Day 编号、每周主题、算法题分布和 EMC++ 条款分布。
- 现有每日工程动作，以及每日 README 末尾“恰好五句复盘”的课程约束。

> **后续扩展（超出 v4.0 原边界）**：本契约成文后，又新增了 [C++ 类与对象核心教程](tutorials/C++类与对象核心教程.md) 和 [STL 容器底层原理](tutorials/STL容器底层原理.md) 两篇专题，补齐经典 C++（类系统、继承多态、虚函数、强制转换、STL 底层）的缺口；并在《CPP_35天科学学习规划_最终版》追加了**可选**的 Week 6（Day 36-42）面试冲刺周。这些**不改动** Day 1-35 的编号、主题、题分布与 EMC++ 条款分布——冲刺周作为独立可选层叠加，主线 35 天结构与本文档契约保持一致。

## 2. 已确认的共性问题

### 2.1 同一内容在多层文档重复

目前常见链路是：

`根总纲 → 专题教程 → 周 README → 每日 README → 形象化指南 → 题目 README`

其中多层会再次完整解释同一语法或同一道题，导致：

- 初学者不知道哪一份是主讲版本。
- 修正一个技术结论时容易遗漏其他副本。
- 每日文档篇幅增加，但“今天新增了什么”反而不清楚。
- 形象化指南、每日 README 和题目 README 的图解、代码与复杂度互相重复。

### 2.2 讲解深度不均

已发现的典型缺口包括：

- 复杂度只给 `O(...)`，没有说明输入规模、前提、最坏/平均/摊还和额外空间。
- 类型推导只列结果，没有区分 `T`、形参类型、表达式值类别和生命周期。
- 智能指针只介绍 API，没有先画所有权、控制块和销毁条件。
- Lambda 只讲捕获语法，没有说明闭包对象、捕获时机、悬空引用和可复制性。
- 移动语义只写“更快”或“置空”，没有说明 `std::move` 是转换、移动可能退化为复制、移动后状态依类型契约而定。
- 并发只给线程 API，没有用 happens-before、谓词、关闭协议和最终不变量解释正确性。
- 算法题有实现但缺循环不变量、终止条件、边界表和复杂度成立前提。

### 2.3 总纲和专题教程职责重叠

- `CPP_35天科学学习规划_最终版.md` 同时复制课表、模板代码、CMake 模板和公共类型实现。
- `Modern_CPP_Features_Deep_Dive.md` 同时重复基础教程中的语法、EMC++ 机制、仓库导读、速查表和七天训练计划。
- `CPP基础学习教程.md` 与 `Effective_Modern_CPP教程.md` 都讲类型推导、智能指针、移动和 Lambda，但学习阶段没有在所有入口处说清。
- 并发教程包含 C++20 扩展，而 Day 29-35 主线是 C++17，版本边界需要更醒目。
- 设计模式和 OS 教程偏“面试答案”，部分结论缺少适用条件、生命周期或平台差异。

## 3. 重构后的单一信息源

| 文档层级 | 唯一职责 | 保留内容 | 不再重复 |
|---|---|---|---|
| 根 `README.md` | 课程入口与最短学习路径 | 环境、周导航、文档角色、验收 | 具体语法和题解 |
| 35 天总规划 | 唯一课表与课程契约 | Day 1-35 分布、先修关系、每日流程、阶段出口 | 大段 CMake/公共类型实现 |
| 现代 C++ 深入手册 | 特性地图与迁移决策 | 版本边界、选型流程、仓库学习路径、诊断表 | 再讲一遍基础语法 |
| 基础教程 | 第一次系统学习语言机制 | 最小前置、核心语法、基础所有权和容器 | 42 条 EMC++ 的工程化展开 |
| EMC++ 教程 | 42 条机制与工程边界 | 错误场景、推导原因、例外、实验 | 零基础语法铺垫 |
| 并发教程 | 并发专题唯一深讲 | 线程、同步、内存模型、关闭协议、C++20 扩展 | 每日树题内容 |
| 项目组织教程 | 工程思维辅线 | 不变量、接口、依赖、所有权、测试与复盘 | 设计模式百科 |
| 设计模式教程 | 模式的动机、代价与现代 C++ 实现 | 适用场景、反模式、所有权、可测试性 | 把模式当固定模板 |
| OS 教程 | 并发所需 OS 背景与面试复习 | 平台模型、调度、虚存、I/O | 把平台惯例写成 C++ 标准保证 |
| 周 README | 一周路线与验收 | 七天关系、先修、周项目、复习表 | 重复每日正文 |
| 每日 README | 当日学习闭环 | 本日差异、最小铺垫、代码导读、工程动作、五句复盘 | 完整复制专题教程或题解 |
| 专题形象化指南 | 手算过程与图解 | 状态图、指针/栈/队列变化、错因图 | 复制所有题目代码 |
| 题目 README | 单题完整题解 | 契约、不变量、正确性、复杂度、边界 | 重复通用数据结构教材 |

## 4. 内容补全标准

每个重要知识点至少回答下列问题；若主讲文档已完整回答，其他文档只写本日连接和相对链接。

1. 它解决什么问题，不使用时会出现什么风险？
2. 标准语义或数据结构不变量是什么？
3. 结论成立需要哪些前提？
4. 对象、引用、指针、迭代器或线程句柄的生命周期怎样变化？
5. 时间复杂度、额外空间复杂度分别如何得到？
6. 哪些情况属于编译错误、异常、未定义行为、有效但未指定状态或仅仅性能不佳？
7. 推荐写法有什么成本和例外？
8. 仓库当天代码在哪里体现该机制？
9. 怎样用预测、反例、`static_assert`、测试或 Sanitizer 验证理解？
10. 它与上一天和下一天是什么关系？

算法题额外补齐：

- 输入输出契约。
- 状态变量或递归函数含义。
- 循环/递归不变量。
- 终止条件与边界表。
- 正确性说明。
- 最坏、平均或摊还复杂度的准确类别。
- 实现选择与替代方案的取舍。

并发内容额外补齐：

- 共享状态和所有者。
- 哪些访问发生冲突。
- 建立 happens-before 的同步边。
- 等待谓词以及虚假唤醒处理。
- 接受、拒绝、排空、停止、唤醒、join 的关闭顺序。
- 结果用协议和最终不变量验证，不依赖 `sleep`、输出顺序或固定耗时。

## 5. 参考资料与引用规则

### 5.1 第一层：标准与权威参考

- C++ working draft：<https://eel.is/c++draft/>
- cppreference：<https://en.cppreference.com/w/>
- ISO C++ Core Guidelines：<https://isocpp.github.io/CppCoreGuidelines/>
- CMake 官方文档仅用于解释文档中的命令语义：<https://cmake.org/cmake/help/latest/>

### 5.2 第二层：优秀教材

- Bjarne Stroustrup, *A Tour of C++*。
- Stanley B. Lippman 等，*C++ Primer*。
- Scott Meyers, *Effective Modern C++*。
- Anthony Williams, *C++ Concurrency in Action*。
- Robert Sedgewick / Kevin Wayne 或同等级算法教材，用于不变量和复杂度组织。
- GoF *Design Patterns*、*Operating System Concepts*、OSTEP，用于专题背景。

### 5.3 使用规则

1. 标准语义、版本、未定义行为和库契约优先由 working draft/cppreference 交叉核对。
2. 设计建议标明它是指南或教材建议，不伪装成语言强制规则。
3. 教材用于组织讲解和选择例子，不大段翻译或复刻原文。
4. 每份主教程末尾提供“主题—资料”对应关系；每日文档只保留与当天直接相关的 2-5 个链接。
5. 对可能随标准版本变化的内容明确标注 C++11/14/17/20。
6. 参考链接不能代替正文：主讲文档必须把资料中的核心模型消化为自己的动机、规则或不变量、前提、反例、边界和练习；仅当这些内容已经完整存在时，引用文档才允许用链接收束。

## 6. 分范围执行计划

### 6.1 Week 1-2：基础、数组、链表与所有权

- 区分 C 数组、`std::array`、`std::vector` 的大小、所有权和越界责任。
- 统一 `auto`、`decltype`、引用和临时对象生命周期的术语。
- 给双指针、滑动窗口和二分补循环不变量与终止证明。
- 给链表操作补“已知节点/需要查找”对复杂度的影响。
- 给归并链表补 K 路实现差异、递归栈和辅助存储。
- 先画所有权图，再讲 `unique_ptr/shared_ptr/weak_ptr` API。
- 解释控制块线程安全不等于所指对象线程安全。
- 题解完整内容落到题目 README，形象化指南保留手算图，每日 README 只保留当天使用方式。

### 6.2 Week 3-4：栈队列、Lambda、哈希与移动语义

- 说明容器适配器的操作约束及其底层容器要求。
- 单调栈明确栈中顺序、弹栈条件和每个元素最多进出一次。
- BFS 明确何时标记 visited 以及重复入队的后果。
- Lambda 补闭包类型、捕获时机、`this`、引用悬空和 move-only 捕获。
- 哈希补等价关系与哈希一致性、负载因子、rehash、最坏退化和迭代器失效。
- 值类别不使用“有名字就是左值”作为完整定义。
- `std::move` 解释为转换；移动是否发生由重载决议和类型能力决定。
- 移动后对象遵守具体类型契约；不统一写成“必然为空”。
- 转发引用先判断是否存在类型推导，再讲引用折叠和 `std::forward`。
- CPU 缓存内容区分标准保证、实现事实和微基准观察。

### 6.3 Week 5：树与并发

- 树题统一写递归函数含义、空节点、叶子定义、递归深度和所有权。
- BST 使用全局上下界说明正确性，不只比较父子。
- 路径问题区分按值状态、共享可变状态和回溯撤销。
- `std::thread` 先讲 joinable 状态和 RAII，再讲任务并行。
- mutex 保护的是不变量，不是某个变量的名字。
- condition variable 始终绑定受 mutex 保护的谓词。
- `future` 区分 `async`、`promise`、`packaged_task` 的共享状态来源。
- atomic 先使用默认顺序建立直觉，再解释 relaxed/acquire/release 的适用条件。
- 线程池和 Actor 明确 close-reject-drain-join 生命周期。
- Day 文档只讲当天并发入口，完整机制链接到并发专题教程。

### 6.4 根文档与 tutorials

- 根 README 改为带角色说明的导航表，补齐设计模式和 OS 入口。
- 35 天规划删除重复实现，保留唯一课表、先修图、学习节奏和阶段验收。
- 现代 C++ 深入手册重构为特性地图、迁移流程、仓库路径和错误诊断，不再与基础/EMC++ 教程逐章重复。
- 基础教程、EMC++ 教程在开头互相说明先后顺序和边界。
- 并发教程把 C++17 主线与 C++20 扩展分开标识。
- 设计模式教程修正不安全的双重检查锁定示例，增加所有权、线程安全和可测试性边界。
- OS 教程修正过度绝对化的平台结论，补充“C++ 抽象机保证”和“操作系统实现惯例”的区别。
- 项目组织教程负责串联每日工程动作，不复制设计模式百科。

## 7. 多 agent 分工

| 执行者 | 独占路径 | 主要产物 |
|---|---|---|
| Agent A | `week_01/**`、`week_02/**` 教学 Markdown | Day 1-14、周导航、数组/链表指南与题解分层 |
| Agent B | `week_03/**`、`week_04/**` 教学 Markdown | Day 15-28、栈队列/哈希指南与移动语义补全 |
| Agent C | `week_05/**` 教学 Markdown | Day 29-35、树与并发的协议化讲解 |
| 主 agent | 根教学 Markdown、`tutorials/**` | 信息架构、教程去重、交叉链接、最终统一与验证 |

分工约束：

1. 各 agent 不修改重叠路径。
2. 开始编辑前先通读本范围，先归纳共因再修改。
3. 只使用 `apply_patch` 修改 Markdown。
4. 不根据旧 CHECK_REPORT 的结论直接下判断。
5. 提交结果前列出修改文件、关键取舍和机械检查结果。
6. 主 agent 统一处理跨范围链接和术语，避免并行产生新的重复。

## 8. 执行顺序

### 阶段 0：计划冻结

- [x] 盘点 Markdown 范围和现有工作区改动。
- [x] 确认只改 Markdown。
- [x] 划分互不重叠的 agent 路径。
- [x] 写明单一信息源和内容补全标准。
- [x] 机械复核本计划没有包含代码修改任务。

### 阶段 1：范围内完整审读

- [x] 建立“主题 → 主讲文档 → 引用文档”矩阵。
- [x] 标记相互矛盾、重复超过一屏、只有结论、缺前提和缺边界的内容。
- [x] 检查现有相对链接、章节锚点和参考资料。
- [x] 记录每日工程动作与五句复盘位置，编辑时保护。

### 阶段 2：按周并行改写

- [x] 先补主讲文档。
- [x] 再把其他重复位置改成“本日差异 + 相对链接 + 练习”。
- [x] 每完成一天，立即检查主题、工程动作和五句复盘未被破坏。
- [x] 每完成一周，检查周 README、七个 Day 和专题指南的职责不重叠。

### 阶段 3：根文档和专题教程重构

- [x] 先确定教程角色和阅读顺序。
- [x] 重构两个根级总纲。
- [x] 对基础、EMC++、并发、项目、设计模式、OS 教程做交叉引用和缺漏补全。
- [x] 删除“如果你希望我下一步……”等对成品教程无意义的对话式残留。

### 阶段 4：跨文档统一

- [x] 统一术语：转发引用、值类别、控制块、有效但未指定、数据竞争、happens-before。
- [x] 统一 C++ 版本标签和 C++17 构建基线说明。
- [x] 统一复杂度表述和“最坏/平均/摊还”标签。
- [x] 统一代码围栏性质：完整程序、上下文片段、反例。
- [x] 统一参考资料格式并减少无关链接。

### 阶段 5：最终验收

- [x] `git diff --name-only` 只出现允许范围内的 `.md`。
- [x] 不覆盖或清理用户原有 `docs/README.md` 和 docs 脚本改动。
- [x] Day 01-35 主题、顺序、算法题和 EMC++ 分布不变。
- [x] 35 个每日 README 的工程动作仍存在。
- [x] 35 个每日 README 的复盘仍为恰好五句。
- [x] Markdown 围栏闭合，表格结构可渲染。
- [x] 所有新增相对链接目标存在；站内链接没有明显断链。
- [x] 根导航、周导航、前后日导航可以形成完整学习路径。
- [x] 不再有根总纲复制大段实现、同题三处完整题解等明显重复。
- [x] 关键技术结论能在参考资料中定位，且标准保证与建议性规则分开表述。
- [x] 不存在“只新增参考链接、正文仍只有结论”的伪补全；主讲位置有完整解释和可验证练习。
- [x] 运行 `git diff --check`，消除穽白错误。

## 9. 完成判定

只有同时满足以下条件才结束本轮：

1. 计划中的所有允许范围完成审读，不以抽样代替全局盘点。
2. 重复内容已经形成明确主讲位置，引用文档仍保留当天学习价值。
3. 已确认的语义、复杂度、生命周期、异常安全和并发缺口得到补全。
4. 每天主题、学习量、工程动作和五句复盘全部保留。
5. 最终差异只包含 Markdown，且不触碰用户已有的无关工作。
6. 所有机械验收通过，并对无法自动判断的教学取舍完成一次人工复核。

---

本文件是本轮修改的执行契约。若实际审读发现新问题，先把它归入上述“单一信息源、内容补全标准或验收门槛”，再做局部调整；不临时扩大到代码或构建系统。
